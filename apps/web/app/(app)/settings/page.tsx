import { PlaceholderPage } from "@/components/placeholder-page";

export default function Page() {
  return (
    <PlaceholderPage
      title="Settings"
      owner="Agent 1 (Foundation) — shell only"
      description="Super Admin only (FSD §9)."
    />
  );
}
