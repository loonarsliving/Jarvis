import { PlaceholderPage } from "@/components/placeholder-page";

export default function Page() {
  return (
    <PlaceholderPage
      title="Users & Roles"
      owner="Agent 1 (Foundation)"
      description="User provisioning against the roles/permissions schema from this Sprint's migrations."
    />
  );
}
