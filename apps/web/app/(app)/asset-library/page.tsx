import { PlaceholderPage } from "@/components/placeholder-page";

export default function Page() {
  return (
    <PlaceholderPage
      title="Asset Library — Browse / Search"
      owner="Agent 5 (Asset Library)"
      description="Full-text/tag/semantic search over assets (FSD §8)."
    />
  );
}
