import { PlaceholderPage } from "@/components/placeholder-page";

export default function Page() {
  return (
    <PlaceholderPage
      title="Pending Review"
      owner="Agent 5 (Asset Library) / Agent 6 (Quality Intelligence)"
      description="Feeds the Review Console (FSD §10 Journey C)."
    />
  );
}
