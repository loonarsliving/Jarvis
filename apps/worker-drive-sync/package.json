{
  "name": "@aaf/worker-drive-sync",
  "version": "0.1.0",
  "private": true,
  "description": "Stub worker service scaffolded by Agent 1 (Foundation). Owner: Agent 5 (Asset Library). See README.md.",
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "dev": "node --import tsx src/main.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aaf/core": "workspace:*"
  },
  "devDependencies": {
    "@aaf/config": "workspace:*",
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
